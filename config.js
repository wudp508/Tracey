// Paste your two Supabase values here, then save.
//
// Find them in Supabase: Project Settings -> API
//   SUPABASE_URL       = "Project URL"
//   SUPABASE_ANON_KEY  = the publishable / anon key (NOT service_role)
//
// The publishable key is safe to publish. The service_role key is not —
// never put that one in this file.
//
// Two names, because they are two different jobs.
//
// APPROVER decides who may read Tracey's journal. That is hers.
//
// CONTACT is who a friend tells when something looks wrong — a need at
// the wrong time, an invitation that never arrived. Tracey has asked not
// to field logistics while she is recovering, so this is someone else.
//
// Both appear in messages friends see. The wording avoids he/she, so any
// name works, including a plural.

window.TRACEY_CONFIG = {
  SUPABASE_URL: "https://stopxvegqiijkpjysnnz.supabase.co",
  SUPABASE_ANON_KEY: "sb_publishable_-A0Bx9Jhi5YpUeRfCcV1ZQ_8FCetQ9X",
  APPROVER: "Tracey",
  CONTACT: "Wud"
};
